# ================= APPLICATION CONFIGURATION =================
spring.application.name=hms-backend

# ===================== DATABASE (MySQL) =====================
spring.datasource.url=jdbc:mysql://localhost:3306/hmsdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=HOSur@2006
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==================== JPA / HIBERNATE ======================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# IMPORTANT: Do NOT specify dialect (Hibernate auto-detects for MySQL)
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ================= SQL INITIALIZATION ======================
# Disable data.sql (H2-only SQL caused crash)
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true


# ======================== JWT CONFIG =======================
jwt.secret=c3VwZXItdmVyeS1zZWNyZXQtZGVtby1rZXktMjU2LS0tLS0=
jwt.expiration-minutes=120

# ======================== LOGGING ==========================
logging.level.root=INFO
logging.level.com.hms=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# ==================== SERVER SETTINGS ======================
server.port=9091
